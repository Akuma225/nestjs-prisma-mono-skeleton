export enum VerifyModeOptions {
    LOGIN_BLOCKED_WHILE_VERIFYING = "LOGIN_BLOCKED_WHILE_VERIFYING",
    LOGIN_ALLOWED_WHILE_VERIFYING = "LOGIN_ALLOWED_WHILE_VERIFYING"
}

export enum VerifyModeOptionsVm {
    LOGIN_BLOCKED_WHILE_VERIFYING = "Connexion bloquée pendant la vérification",
    LOGIN_ALLOWED_WHILE_VERIFYING = "Connexion autorisée pendant la vérification"
}