export enum ModelMappingTable {
  USER = 'users',
  PRODUCT = 'products',
  CATEGORY = 'categories',
  PASSWORD_RESET = 'password_resets',
}

export enum ModelMappingPrefix {
  USER = 'USR',
  PRODUCT = 'PRD',
  CATEGORY = 'CAT',
  PASSWORD_RESET = 'PSW',
}
